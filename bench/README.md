# 投机执行验证与消融

验证以同一机器、同一初始工作区和真实生产入口为准。完整依赖矩阵见[能力与验收边界](../docs/bash-reuse-capability-lattice.md)；下列单项通过不代表所有消融行均已完成。

私有录制、预注册材料和每次运行的原始 JSON 保留在仓库外；仓库只保留可复现脚本及经过审阅的结果摘要。

## 文件工具采纳延迟

`adoption-latency.mjs` 从完整 `Host.execute` 入口计到 Actor 结果返回，分别报告准备时间、原生调用和 fallback；不把内部 `hitLatencyMs` 当成完整交付时间。使用真实 read/ls/write/edit 和共同绑定的 captured find/grep，确定性模型只提供已知动作，API 请求数为零。

```sh
npm run build
node bench/adoption-latency.mjs . /absolute/output/ready.json 20 ready read,ls,write,edit,find,grep,native-find,native-grep
node bench/adoption-latency.mjs . /absolute/output/running.json 3 running read,ls,write,edit,find,grep
```

每次新建 Host，核对同路径原生输出、文件内容与 POSIX mode，禁止提前写入；采纳与回退分别检查执行次数。`running` 在 Actor 加入后释放受控生产者，单列剩余执行与完成后的交付时间；它验证运行中复用，不代表自然任务加速。报告分别保留命中、回退时长和拒绝原因，不能把正确回退当成测试失败，也不能把所有调用的 p50 当作命中 p50。

Linux/WSL 使用真实进程后端；既有 helper 可通过 `PI_SPEC_SANDLOCK`、`PI_SPEC_HELD_EXEC` 指定，不自动安装。`bash-child` 的父命令不同，共同调用编译后的 CPU/文件效果夹具，Actor 经过真正 held-exec 边界消费子进程。默认并发额度 2 允许接入运行中工作；`--concurrency=1` 验证抢占后的单次回退。子进程就绪时间取自 handoff 完成，不能使用稍后结束的整个父分支时间。

```sh
node bench/adoption-latency.mjs . /absolute/output/bash.json 15 ready bash,bash-child --backend=linux-process
node bench/adoption-latency.mjs . /absolute/output/child-running.json 3 running bash-child --backend=linux-process
node bench/adoption-latency.mjs . /absolute/output/child-preemption.json 2 running bash-child --backend=linux-process --concurrency=1
```

ThinkThread 要在真实 Profile 的 `THINKTHREAD_FS` 根目录运行并通过控制连接检查，使用 `--backend=thinkthread` 和 `read,ls,write,edit,native-find,native-grep,bash`；输出路径也须由 Profile 允许写入。使用生产默认封存输入/runner 路线。write 的 EACCES 回退、edit 的既有权限/身份缺口和不支持的进程路线照实报告；内容/mode 比较不能授予完整写入资格。

`--profile` 临时插桩分段调用，`--fs-profile` 进一步统计 I/O。`producerSpans` 记录准备及生产调用，`spans` 记录 Actor 调用；broker 和 held-exec socket 回调分别归各自所有者。running 模式的生产记录会延续到 Actor 到达之后，不能把这些时长当成到达前的准备时间，也不能相加嵌套或重叠时长。性能结论使用不插桩的运行。`--resource-baseline`、`--workspace-baseline`、`--handoff-baseline` 接受对应旧构建模块的绝对路径，用于同一驱动下的相邻对照。结果文件必须不存在。

2026-09-12 的各 20 次相邻对照中，Windows x64 新文件 write 的完整采纳 p50 为 3.89 → 3.33 ms，captured grep 为 4.96 → 3.74 ms；WSL x64 grep 为 4.43 → 4.06 ms。一次 grep 诊断的 lstat/realpath 调用由 151/100 降到 117/66，新文件 truncate 由一次降到零。其他小文件工具没有一致的净改善；这些数值不是自然应用端到端加速声明。

同日 WSL 子进程 ready 15 次命中 p50 为 16.07 ms，原生重算 96.72 ms；旧模块相邻对照的 11 次命中 p50 为 16.84 ms，随后一次文件系统时钟证明失败并正确回退，未完成等量时延对照。因此只确认同轮已完成/加入后完成的结果省去一次持久历史查询，不声称稳定的整体提速。两版都出现时钟证明失败，失败不隐去。ThinkThread 两个 ready read 样本为 20–34 ms、ls 为 15–29 ms，原生均不足 1 ms；仍有明显采纳开销。

## PatternAware 历史与绑定

```sh
node bench/pattern-latency.mjs . /absolute/output/pattern.json /absolute/old-pattern-aware.js
```

旧模块须来自同一依赖版本的构建；省略时只测当前版本。默认比较搜索结果宽度 0、96、512 的三组历史，每组五轮交替顺序，逐项核对预测前沿和最终学习状态。可追加真实事件 JSON 路径：扁平项沿用 `observe`，显式数组项使用 `observeBatch`，不把同批工具误当连续决策。计时包含学习、预测、快照和结算，单列 observe/predict；它是组件对照，不发 API。

2026-09-12 Windows 的独立运行中，96/512 项结果的预测中位数分别为 10.58 → 10.10 / 36.17 → 34.63 ms；包含学习的总时间为 112.16 → 108.79 / 292.10 → 296.51 ms。后缀共享与批次起点减少分配，但总时间没有一致改善，不能据此声明自然任务端到端提速。Windows/WSL 的 74 项相关回归均通过。

随后共用批次输入与历史 token 的重构，对真实录制的 20 个工具/14 个批次及上述三组历史逐项对照 `0da88d5`，全部前沿与学习状态一致。真实历史总时间为 23.38 → 23.95 ms，没有稳定提速；这项归类为重构。Windows/WSL 各 74 项回归包含多工具假设批次、调用方修改输入及批次因果边界。

相对 `6e743a5`，绑定的多个字段组合先排序、截断，再构造入选输入；原概率、相同概率时的组合顺序与缺失路径处理保持一致。五轮交替组件对照中，96/512 项结果的学习到结算总时间中位数在 Windows 为 113.80 → 95.99 / 301.95 → 221.09 ms，在 WSL 为 98.78 → 88.64 / 290.28 → 208.37 ms。全部预测前沿和学习快照逐项一致，另有 1,152 个深冻结输入的相邻版本对照覆盖嵌套数组、缺失/禁用路径、同权重、靠后高权重、非有限计数及公开 limit 行为，两平台各 140 项相关回归通过。真实录制历史仍无一致改善：Windows 23.02 → 24.50 ms、WSL 20.46 → 20.20 ms；不把宽搜索结果中的组件收益推广为自然任务加速。

提案和候选物化通知的可选元数据共用复制操作后，Windows 五轮交替组件对照中，每样本 128,000 次构造的中位数分别为 143.48 → 120.07 ms、187.56 → 156.66 ms。4,096 个差分案例核对完整值、字段顺序、属性描述符和输入引用，覆盖缺失、undefined、零值及非有限值；实际调用中的开销很小，这约 16% 的组件改善不等于完整 Host 或任务提速。

## 受控搜索资格

先完成 `npm run build`，再按需要选择低负载命令：

```sh
node --experimental-strip-types bench/grep-captured-qualification.mjs --semantics-only
node --experimental-strip-types bench/grep-captured-qualification.mjs --links-only
node --experimental-strip-types bench/grep-captured-qualification.mjs --cost-only --case=repository
node bench/portable-kernel.mjs
```

- 语义模式每个平台覆盖 46 个小型案例：分层 ignore、原生 glob 优先级、显式被忽略根、UTF-8/UTF-16/二进制原始输入、完整 Pi 格式化、上下文回读与截断。
- 输入、ignore 或负查询变化后，必须回到同 profile 的原工作区 Actor 且恰好执行一次；被忽略的 16 MiB 稀疏文件不能进入内容证据。
- 真实 rg 的取消/结果限额 barrier 必须证明原生关闭和借入输入清理先于结算。已退出的小进程不必再次被杀死，尚被借用的操作仍要等待取消。
- `--case=<标签>[,<标签>...]` 选择案例。链接特征化使用 16 次进程调用，分别检查 `--files` 与实际搜索；不能仅凭文件列表跳过会让搜索报错的 dangling junction。
- 覆盖具名同卷链接、POSIX 链接 ignore 文件、祖先配置、Git 间接路径和显式 Git 数据搜索。HOME、`@`、绝对路径、file URL 与编码空白使用 Pi 原解析器。
- 断裂目标、跨卷输入、cwd 别名与 glob 组合仍未取得资格；原工作区 Actor 保留原始行为及错误。macOS/ARM64 尚未验收。

生产 `createClosedSearchProfile` 同时提供 producer 和 Actor，资格脚本不维护另一份物化器。它只接受已有 Windows x64 rg 15.2.0 或 Linux x64 rg 14.1.0，固定可执行字节、SHA-256 和所属私有副本；不安装或下载。缺少 rg 可以跳过对应资格，不能把不支持的引擎当作成功。

元数据和 ignore 证据驱动 rg 自行选择文件，只复制选中的原始字节。目录按需展开，以私有唯一 marker 检查下一个浅层前沿；不自行解释用户 glob，不枚举被忽略/负 glob 子树，也不隐式遍历 Git 对象。生成规则在执行前移除，被选中配置恢复原始内容。

具名祖先配置可捕获到声明的只读卷根，但不会枚举祖先或递归监听整卷。私有路径几何保留锚定规则，`gitdir`/`commondir` 只指向自有控制文件；新增原先缺失的 commondir 或改动 exclude 会使采纳失效。物理配置身份与逻辑查询拼写分开保存。保持原生仓库识别，不使用会破坏 Git 间接路径的 `--no-require-git`。

该 profile 共同绑定 Actor 与投机的 HOME、原始已准备参数、路径排序及禁用环境 rg/全局 ignore 的设置；祖先 ignore 仍是输入。只在已经物化完整的私有命名空间禁用原生父级加载。它不是 Native Pi 默认等价，也不是任意 Bash 或恶意 JavaScript 沙箱。

`--cost-only --case=repository` 使用约 1 MB 输入、三个查询、每个查询三次采纳尝试。同一个 Host 先关闭预测、实测原 Actor；fallback 必须执行一次，并如实报告 `candidate_join_not_profitable`。不修改收益门或强迫命中。未筛选的默认模式含更大输入、每查询五次采纳及 20 次取消/限额 barrier，仅用于性能阶段。

Native Pi、固定排序 Pi、profile 准备、producer、worker 启动和 Actor 到达后的采纳分别计时。小语义/TUI 夹具的耗时不构成加速声明。报告中的进程计数只覆盖被插桩的参考 worker 与取消探针，不包含普通 Pi 基线或所有生产 profile 进程。

`portable-kernel.mjs` 使用完整原版 Pi find、输入代理、独立 Actor/producer 进程、现有 Runtime 和真实生产 TUI 回调，仅模型与界面输入由脚本提供。它覆盖完成/跨轮次采纳、封存输入重算、running join、并发 producer、名字查询与内容预算、ignore/链接变化、单次 fallback、关闭时排空 Actor 和取消 producer。

deadline/abort 还覆盖已进入的不合作循环及拥有独立进程的输入操作；工作进程和输入回收后才允许 fallback，晚到结果必须被消费。V8 堆限额不是整个进程的 RSS 保证。原生 fd 与共同配置 Actor 分别采样，不用更换执行语义制造收益。

## Linux/WSL 进程复用资格

前提是所选本机组件已通过资格探测；安装与分级依赖见[主说明](../README.md#安装与运行)。可复用已有离线构建，无需为测试重新安装。下面的进程基准支持用 `PI_SPEC_SANDLOCK`、`PI_SPEC_HELD_EXEC` 显式选择已有 binary；这不是默认扩展的环境变量配置接口。

```sh
npm run bench:linux-process -- --output /absolute/output/child-reuse.json
npm run bench:linux-inflight -- --output /absolute/output/inflight.json
```

进程基准经过真实 `createBashTool`、通用进程出口、Linux world、工作区分支、采纳前新鲜度验证和 commit。先比较直接执行与生产 tracing 形态的完整输出、退出和文件效果，报告 trace 成本、污染及严格证书资格；再验证 Actor 消费者不能误接不兼容沙箱证书，并要求同父/不同父 Bash 的子进程命中，改变动态输入后必须 miss。

in-flight 基准让 Actor 在观察 PID 的 Bash 候选运行中到达，再重复同一个 Actor 调用。Runtime 必须仅接管一次；第二次正常执行，单次产物不得进入持久历史或另一份顶层接管表。测试绕过模型请求只是为了固定动作身份，不代表模型质量或端到端工作负载收益。

相对 `6e743a5`，PATH 准备的确定性取消回归用 33 个可执行文件与首批探测 barrier 验证所有权：旧版取消后仍开始 18 次探测并启动 broker；当前版先排空已接纳操作，停止后续批次，并在返回前回收工作区。五套相关回归在 Windows 为 42 通过、11 项平台跳过，在匹配 helper 的 WSL 为 53 通过、零跳过。这是减少无用准备与保证取消边界的证据，不是自然任务加速测量。

以下大输入命令仅用于专门的成本阶段：

```sh
npm run bench:linux-pathset -- --output /absolute/output/pathset.json
npm run bench:linux-artifacts -- --output /absolute/output/artifacts.json
npm run bench:linux-topology -- --mode direct --output /absolute/output/topology-direct.json
npm run bench:linux-topology -- --mode reuse --output /absolute/output/topology-reuse.json
```

| 命令 | 必须证明的事实 | 成本边界 |
| --- | --- | --- |
| `linux-pathset` | 一个 32 MiB 输入的八个历史状态都可参与查询，相同路径集只捕获一次当前依赖 | 不按历史证书重复哈希大输入 |
| `linux-artifacts` | 三个父命令的 128 MiB 文件效果重放均命中，摘要一致；开始效果前验证完整产物闭包 | 单独记录校验产物数量与字节 |
| `linux-topology` | 子进程创建两个目录和 32 MiB 确定性产物；三个父 Bash 恢复精确目录/文件状态，外层验证并提交 | direct 是收益对照，保留重放更慢的短任务 |

2026-09-12 复核发现，同轮交接逐条调用规划器，让上述八历史状态基准在原版脚本也因重复捕获而失败。改为批量选择后，八个候选共用一次当前路径集捕获，验证读取从 285,632,152 降到 35,704,019 字节；完整输出及产物一致。并发整支/子进程消费发生在验证期间时，失去认领权的结果被拒绝，剩余候选重新验证。此处确认读取量改善，不作独立端到端提速声明。

路径集与产物闭包共用 `linux-process-cost.ts`，原 npm 命令保留。公共计时等待分支 `dispose` 完成；夹具类型、指标差值及 C 写入循环共用一处实现，四份生成的 C 源码与整理前一致。128 MiB 产物的三次命中、基础跨父命令复用、`--rounds 0` 的三次拓扑恢复及运行中结果的一次性接管均通过。基础复用首轮因文件系统时钟未越过事务边界而拒绝，原始失败保留，未修改证明或延长等待上限，后续完整重跑通过；基准整理本身不作性能收益声明。

相对 `fa914a4` 的后续优化让一次 handoff 查找记住已尝试的证书身份，持久层在读取证书文件前跳过同一对象；未尝试过的历史仍独立验新，后续 acquisition 不继承排除集合。没有复用旧的验证证据或扩大 taint 接受范围。原 32 MiB、八种输入状态加一次最早状态命中的夹具中，三次运行均将验证路径集从 15 降至 8、读取量从 535,560,285 降至 285,632,152 字节（减少 46.67%），证书候选次数从 64 降至 36。完整输出、产物、冷执行与命中计数一致。

| 相邻对照 | 原九次执行合计 | 优化后合计 | 原查找验证合计 | 优化后验证合计 |
| --- | ---: | ---: | ---: | ---: |
| 1 | 24.389 s | 23.188 s | 430.22 ms | 201.16 ms |
| 2 | 23.035 s | 22.863 s | 423.41 ms | 196.97 ms |
| 3 | 22.964 s | 22.974 s | 415.27 ms | 193.78 ms |

验证时间中位数降低 53.48%，完整执行没有稳定净改善；合计包含每次分支的准备、执行、验证、提交与回收，不包含夹具编译及后端初始化，也不是完整 Agent 任务时间。首次优化版运行因文件系统时钟落后事务边界约 647 ms 而失败；日志保留，同参数重跑作为第一组优化样本，没有修改 100 ms 时钟证明预算。Windows/WSL 回归覆盖只在本次查找排除、剩余磁盘历史、查询期间完成及所有权竞争。

拓扑基准的 `--rounds N` 范围为 0–4096、默认 96；两侧使用同值扫描交叉点。旧报告驱动的 `linux-admission` 分析入口已移除；收益决策由 `test/scheduler.test.ts` 验证，实际加入/回退使用前述完整 Host 采纳基准。

进程与拓扑基准支持 `--workspace-driver git` / `overlayfs`，用于同机器 A/B；`--source-files N` 扫描目录规模。auto 只有在 binary、FUSE、copy-up、whiteout、opaque 目录、匿名事务时钟、跨视图时间顺序、可见性及卸载均通过，且精确基线至少 256 项时选择 OverlayFS。小树保留 Git；驱动导致的不支持结果会污染证据。`routePreparationMs` 与 fork/hit 分开报告。

```sh
npm run bench:overlay-probe
npm run bench:exec-boundary -- --output /absolute/output/exec-boundary.json
```

Overlay 探针描述宿主非特权能力，不是生产路线切换许可。生产 FUSE upperdir 中的 whiteout/opaque 记录必须按类型化前沿解码，不能当普通目录合并。exec 边界基准分别计量仅 fork/exec 事件的 ptrace、普通进程过滤 strace、seccomp-BPF 加速，并验证 x86-64 held exec 在耗时子进程开始前可被替换，同时记录可观察的 `TracerPid` 差异。

随后整理 exec 分支、Actor 计时和等待助手，生成的 1,735 字节 C 程序及 17 个命令字面量与 `fa914a4` 完全一致；completed/running、同轮一次性消费、跨轮拒绝、描述符、逻辑 cwd 和元数据检查均保留。一次原生运行在“completed child did not remain ephemeral”计数断言失败，原日志没有详细计数，原因未确定。只补充错误计数信息后，两次当前版和一次 `fa914a4` 复核通过，running join 基准也通过；未改变等待预算、领先时间或证明条件。这次未复现失败仍属保留结果，不据重跑宣称不存在间歇问题。

后续确定性屏障审计发现独立的捕获生命周期缺陷：在 `6e743a5` 中，trace 读取或事务封存一侧抛错，另一侧仍被阻塞时 trace 目录已开始清理。两种顺序均复现；修复保留并行捕获与首个错误，失败后排空双方再清理和返回已执行输出。回归核对单次执行、目录回收、禁止发布与采纳，Windows 42 项通过、10 项平台跳过，匹配 helper 的 WSL 52 项通过。这解释了一条捕获失败路径，尚不能认定是上述偶发计数断言的根因。

## ThinkThread 真实 Runtime 资格

使用已安装真实 Agent POSIX SDK、已构建的源码 checkout 和运行中的 `tt pi-speculative-action` Profile。在 `THINKTHREAD_FS` 根目录通过 Pi 的 Bash 工具运行：

```sh
node bench/adoption-latency.mjs . /allowed/output/thinkthread.json 3 ready read,ls,write,edit,native-find,native-grep,bash --backend=thinkthread
```

创建夹具前通过 SDK `selfView/fs.stat` 确认 Runtime，环境变量不足为证。统一使用完整 Host 和生产默认执行路线；显式覆写 runner 的旧入口已移除。本地 wire runner 的六工具语义对照保留在 `test/thinkthread-tool-runner.test.ts`，不是 Runtime 通过证明。当前采纳测量与权限/身份限制见本文开头。

本机 WSL2 x86_64 使用 alpha4 RPM `0.1.0-19` 解包布局、Pi 0.84.1/Node 24.20.0：`read/ls/edit` 采纳通过；新文件 `write` 令整条命令失败。原生 Node 对照确认，不存在路径的异步 `realpath` 在私有分支报 `EACCES`，Actor 报 `ENOENT`。保留权限错误，Host 拒绝候选后 Actor 成功写入一次。

SDK 在首次准备或指纹检查时加载；缺失 SDK 时注册成功，自检报告不可用，真实 Actor 仍完成一次原生调用。根普通文件使用封存输入与原版 Pi 绑定；其余输入保留 `fs.run`，两者共用请求/响应预算。既有权限、祖先绑定和图像 Worker 回收资格对应 `10bec82`；本次读取与编辑的首次准备、采纳、效果及请求/快照回收另经真实 Runtime 对照。

TUI 修改须 Apply；开放 helper 读取后，嵌套 ptrace 仍报 `EPERM`，Bash 提前执行不可用，Actor 回退正确。文件路线、wire 和快照检查不授予嵌套 tracing/handoff、Host Checkpoint/restore、ARM64 或完整进程闭包资格。

## 录制与模型消融

外部 `pi-llm-tape` 仅将完整消息上下文相同的 Actor/Drafter 请求配对，工具名和全部参数须精确匹配：

```sh
npm run bench:tape -- \
  --tape /private/path/deepseek.json \
  --actor-model deepseek-v4-pro --drafter-model deepseek-v4-flash
```

报告区分原始/唯一 K(a)、重复请求、精确命中、Actor 完成前就绪、解码领先及 Drafter 总服务时间，并按派发顺序对宽度 1、2、3、8 对照。请求成本按 Actor 决策计一次，动作覆盖单独计量，不能让并行工具重复放大请求费用。

竞速统计也按获胜响应的完整可解码工具批次计算，重复动作增加候选数量，但不重复增加覆盖；空响应不取消同伴，只有获胜后剩余服务可计为模拟节省。旧实现仅计首个工具，参数化多工具场景已复现漏计。96 组包含空响应、重复动作、多工具、晚到和乱序响应的独立对照中，普通动作、宽度、收益门及重试分析与 `fa914a4` 逐项一致；竞速的完整/获胜批次覆盖另用原版宽度计算验证。这里分析可解码录制，不代替生产请求的工具资格和执行绑定检查。

请求返回后的准入也属于准备生命周期。确定性 Host barrier 复现了有效 Drafter 批次被准入拒绝后仍继续持有预热的问题；Runtime 现在让一次请求及其候选共用一个令牌，最后一个持有者退出时退休。原有竞速回归增加了空响应、无效绑定和异常绑定的退出原因检查，存活同伴与获胜候选继续持有；相关 Windows 193 项、WSL 268 项回归通过，另补充缺少必需参数的 Host 检查。首轮 Windows 完整重放保留 25 个工具、22 次采纳与每个角色 20 次请求，所有运行状态清零；没有据此计入 API 或费用节省。

Drafter 的一个响应可提供一批工具预测；整批结果按响应顺序反馈，只占一次后续模型请求。`run.ts` 的 `drafterPredictions` 保存完整 `calls` 与每次请求的 `usage`，不再只记录第一项。Windows/WSL 的 Host/Runtime 170 项相关回归覆盖单请求额度、逆序完成、重复反馈及 thinking 继承。真实录制中确认八个正常完成的多工具响应旧版丢掉十个调用；这是丢失预测的诊断，尚非修复后的自然任务准确率或端到端加速证据。

`0da88d5` 的长程 mock 经真实 Agent/Host、文件工具及 Bash 校验完成 14 次工具决策、20 次调用和最终回复。相同完整请求使用 `pi-llm-tape` 严格重放；Actor/Drafter 固定延迟为 400/80 ms，候选额度 1，搜索在 Actor/producer 两端共同绑定 captured profile，外部 API 请求为零。每种配置三轮交替顺序，完整工具输出和最终工作区一致：

| 平台/后端 | 关闭投机中位数 | `99fc50a` Drafter | 完整批次 Drafter |
| --- | ---: | ---: | ---: |
| Windows/local | 8.165 s | 7.887 s | 7.876 s |
| WSL/local | 7.313 s | 7.197 s | 7.167 s |

完整批次把 Drafter 请求从 17 降到 15，精确匹配动作从 14 增至 20，Drafter 采纳从 11 增至 17。两版 Drafter 的已观察预测精度均为 1；改善的是动作覆盖和请求数，旧/新耗时差不足以证明稳定提速。相对关闭投机约 3.5%/2.0% 的改善只属于这组构造响应与时序，mock usage 也不代表实际 tokens 或费用。

持续错误预测的完整任务对照中，现有收益门将 Drafter 请求从 15 降到 6、跳过 9 批；单次时长 8.194 → 8.339 s，不据此声称提速。正预测对照保持 15 次请求，未知 Actor 成本没有被伪造为零。Linux process 的同一只读流程单次关闭/开启为 7.436/7.796 s，子进程命中为零；文件命中不能计为 Bash 复用。原始自然模型运行在 14 轮上限结束且 native grep 截断次序导致严格重放失败，保留作准确率诊断，不纳入完成任务的性能比较。

`e76ecae` 的后续 mock 增加 write/edit、逐步文件状态检查和最终回复，WSL 另含跨父命令 C 子进程。使用合格工作区、同一完整 tape，三轮按 off/all、all/off、off/all 顺序严格重放；完整工具输入、输出及最终状态均一致。计时包含 Host 初始化、全部执行及回收，外部 API 请求为零：

| 平台/后端 | 工具决策/调用 | Actor/Drafter 固定延迟 | 关闭中位数 | 开启中位数 | 延迟减少 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Windows/local | 19/25 | 400/80 ms | 10.410 s | 10.326 s | 0.81% |
| WSL/linux-process | 21/27 | 2500/80 ms | 57.282 s | 56.763 s | 0.90% |

Windows 每轮 22 个文件工具采纳，但首次 edit 的运行中等待中位数约 353 ms，原生约 3 ms；这不是单纯的采纳交付成本，也不是所有工具都有净收益。WSL 每轮 23 个文件工具采纳、1 个跨轮次子进程命中：Actor 自己的父 Bash 保留执行，整次 Bash 返回中位数 852.02 → 32.15 ms（26.5 倍），子进程效果只提交一次。C 程序对小文件循环计算 1,000,000 次，不能当作自然应用工作负载；22 次 Actor 请求本身固定占用 55 s，局部收益没有等比例转化为端到端收益。

后续相邻版本对照定位到 Windows edit 的等待来源：增量刷新执行 `read-tree` 时丢失未变条目的 stat 数据，导致反复重建相同基线。改用保留匹配条目状态且不触碰工作区的 `read-tree -m -i` 后，在相同 400/80 ms 轨迹上顺序平衡的三组 `bc9f7b9`/修复版独立运行中，edit 完整返回中位数 327.03 → 4.41 ms，完整任务 10.324 → 9.965 s（缩短 3.48%）；write 为 4.13 → 4.72 ms，未改善。六次完整工具输入、输出、后续上下文、逐步文件状态和最终回答一致，每次 22 个文件采纳；未改变等待策略或新鲜度校验。合并后的回归在 Windows 通过 27 项、跳过 4 项平台限定用例，WSL 通过 31 项。[Git 单树合并语义](https://git-scm.com/docs/git-read-tree#_single_tree_merge)说明了保留索引状态的行为；构造模型时序的测量边界仍适用。

1500/80 ms 的同流程诊断中，子进程生产链约 3 s，超过约 2.1 s 的领先时间，仍安全回退；没有修改作用域、描述符、新鲜度或效果证明来制造命中。审计另修复了调度器将未知服务耗时当作 1 ms、推导虚构 Actor 周期的问题，Windows/WSL 各 191 项相关回归通过；上述开关对照不单独证明这项修复的性能贡献。早期只读夹具的 node_modules 链接越出工作区，不能用来证明 Linux 提前执行已具备资格。

继续审计该 1500/80 ms 轨迹，发现私有 Git 快照排除了 `.git`，基线验新却仍扫描其中的对象。将验证范围对齐后，诊断中每次读取由 442 个文件降至 202 个。三组不插桩、按旧/新、新/旧、旧/新顺序的独立对照中，完整任务中位数为 36.348 → 34.962 s（缩短 3.81%），跨父命令 Bash 为 1000.31 → 93.84 ms；旧版三次均未赶上子进程结果，新版三次均命中，Actor 父 Bash 每次仍执行一次。对照使用 `adebd12` 的工作区模块关闭排除选项，其余模块完全相同；没有延长模型领先时间。六次完整请求 payload、27 个工具的输入/输出、后续上下文、逐步文件状态及最终回答一致，Actor/Drafter 均为 22 次请求，外部 API 为零。此收益仍限于这组构造响应、CPU 夹具与时序。根目录和嵌套元数据、普通 `.gitignore`、调用方修改排除列表、可见链接进入排除目录及局部快照权限均纳入现有回归；Windows 49 项通过、10 项平台跳过，WSL 59 项全部通过。

随后合并同一准备信号内仍在进行的基线预热，该流程两次诊断的验证次数为 73 → 41，读取量约 204 → 114 MB；这部分工作原本大多与 Actor 重叠。不插桩的完整 WSL/Windows 复核分别为 34.958/10.042 s，完整请求与工具结果仍一致，不单独声称稳定的端到端提速。共享只存活到当前预热完成或失败，合并回归覆盖独立服务、取消、失败重试及同一信号后续文件变化；实际 fork 始终重新验证。

批量交接修正后的最终完整复核为 Windows 9.994 s、WSL 35.076 s；分别完成 25/27 次工具调用和每角色 20/22 次严格匹配的请求，完整输入、输出、请求 payload、原生调用次数、逐步文件状态和最终回答与前次一致，运行状态计数全部归零。PatternAware 的 19/21 个真实记录批次保留全部成员和因果边界；同批到达顺序的差异经实际 Store 在相同服务成本下重放，所有后续预测与学习快照一致。没有新增外部 API 请求，也不据此宣称自然预测准确率或费用改善。

本轮还复现了两个 Drafter 准备缺陷：全部候选在模型选择阶段取消后，旧版仍调用配置函数；配置沿用首个候选的信号，又会误伤仍存活的同伴。模型/配置准备和预热改由整批存活候选共同持有，最后一个退出或轮次结束时统一退休，已启动且不可取消的操作仍排空。Windows/WSL 各 162 项生命周期回归通过；没有把减少无用配置调用称为自然模型请求或费用下降。

后续又在 `6e743a5` 复现了整批没有可用动作仍继续预热的问题。准备与提案结果现在共享一处持有逻辑：空/无效批和失败响应释放自己的持有，有效提案与未完成同伴继续持有，包含两个请求使用同一取消信号的情况。Host 屏障证明空响应在 Actor 到达前退休预热，关闭仍等待已经开始的准备操作；两平台各 204 项相关回归通过。一次真实 Git 预热的受控对照在首次 lstat 后交付空 mock 响应，旧版随后完成 10 个原生命令、23 次 lstat 和两个临时目录创建，新版在同一屏障后停止，仅保留已完成的一次 lstat，两侧均完整清理。两版都发出一次 mock 请求，外部 API 为零；这证明无用准备减少，不代表实际 API 请求或费用下降。

这些修改后的完整回放为 Windows 10.002 s、WSL 35.108 s，仍分别完成 25/27 次工具和每角色 20/22 次请求。逐项核对上述完整轨迹与 `fa914a4` 最终记录，全部一致；Windows edit 完整返回为 4.19 ms，WSL 跨父 Bash 为 99.47 ms、子进程命中一次，原父命令执行一次。PatternAware 原始同批顺序在 WSL 有差异，19/21 个批次的成员、边界以及实际 Store 在相同服务成本下的所有前沿和学习状态仍一致。最终相关回归为 Windows 378 项通过、14 项平台跳过，WSL 392 项全部通过；代码与基准类型检查、构建通过。初始化、执行、回收包含在完整耗时内，所有运行状态计数归零；这轮没有新的稳定端到端提速证据，mock 时序和 C 计算夹具不代表自然模型效果。相对 `fa914a4`，`src/test/bench`（含 C，排除文档）合计减少 211 个实际代码文件行、10 个规范化 LF 字节。

本轮相对 `bc9f7b9`，`src/test/bench` 的代码文件（含 C）净减 208 行、29 个 LF 字节：src 增加 1,609 字节，test 增加 4,742 字节，bench 减少 6,380 字节；文档不计入。新增覆盖合并或参数化现有测试，保留有效用例。

### 以 `6e743a5` 为基线的持续审计

捕获与准备生命周期修复、PatternAware 有限前沿优化后，两平台分别进行旧/新、新/旧、旧/新三组独立重放，再对最终 `20a8614` 的生产代码复核一次。全部使用相同工作区、完整 tape 和原有 mock 时序，包含初始化、执行、效果交付与回收；外部 API 请求为零。

| 平台 | `6e743a5` 三次中位数 | 修复后三次中位数 | 最终完整复核 | 工具调用 | Actor/Drafter 请求 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Windows/local | 10.041 s | 9.984 s | 10.039 s | 25 | 20/20 |
| WSL/linux-process | 35.034 s | 34.916 s | 35.046 s | 27 | 22/22 |

各轮完整输入、输出、后续请求 payload、请求阶段、thinking 内容摘要、tool choice、token 预算、usage、逐步文件状态与最终回复均一致；原生调用次数相同，所有运行状态归零。Windows/WSL 分别保留 22/23 次文件采纳，WSL 跨父命令 Bash 最终返回为 102.71 ms，Actor 父命令原生执行一次、子进程复用一次。Windows edit 三次中位数为 4.26 → 3.99 ms，最终样本为 5.93 ms；关闭投机的完整返回为 3.89 ms，其中原生执行 3.24 ms。没有新的稳定端到端提速证据。首次 WSL 对照缺少已构建的基线 dist；该不完整序列及补建后的全部结果仍保留，其中一对为 34.848 → 36.498 s，不用后续通过或较快样本覆盖这次负收益。

19/21 个 PatternAware 批次的成员和因果边界逐项相同。核对成员时只排序副本；学习验证保留每次原始到达顺序，实际调用 Store 的 observeTurn/observeBatch/predict，仅将 durationMs 固定为 1。后续所有前沿与最终状态一致，分别得到 13/12 个模式和 38/47 个累计前沿预测。全部来源开启时，PatternAware 没有贡献额外 Actor 采纳；最终样本还保留了 Windows/WSL 的 7/24 条失败候选事件。关闭 Drafter、保留 PatternAware 与 Actor 预览的完整复核为 10.692/36.323 s，仍与基线的完整 Actor 轨迹一致，两端各有两次 `resource.inputs` 读结果投影复用，精确预测匹配仍为零。其完整耗时高于单次关闭对照的 10.475/35.165 s；这些开关样本用于检查来源贡献，不证明自然预测或费用改善。

生产者与 Actor 分别插桩的 Host 样本中，ready Bash 准备/完整返回为 1848.10/19.49 ms，原生为 5.90 ms；ready 子进程准备/整个父 Bash 返回为 2423.66/20.40 ms，原生为 96.24 ms。运行中子进程的完整父 Bash 返回为 437.14 ms，其中剩余生产者耗时 419.77 ms、生产者完成后至返回 17.36 ms，原生为 101.10 ms。两种子进程样本均保留 Actor 父命令执行，分别记录一次已完成采纳/一次加入；嵌套 span 不能相加作为完整任务耗时。最终独立 exec 边界与 Runtime 运行中采纳检查通过，保留同轮限定、描述符拒绝、改变输入回退、一次消费和禁止持久发布的证明。4 秒 sleep/3 秒领先的运行中夹具仅为机制验证，未改变原有预算，也不代表自然领先时间。

最终 Windows 全套回归为 617 通过、20 项平台跳过；匹配 sandlock/held-exec 的 WSL 为 636 通过、1 项 Windows 路径规则跳过。两端代码/基准类型检查与构建通过。WSL 验证副本残留旧文件的失败日志保留；同步后暴露的基准类型导出错误也在 `6e743a5` 复现，返回边界保留公开 AgentTool 类型后通过。类型差分证明其只读形状相同，编译后除等价属性简写外的 JavaScript 相同。夹具、物化计划及配对统计重构保留全部断言；`src/test/bench`（含 C，文档除外）相对 `6e743a5` 净减 192 行、124 个规范化 LF 字节，分目录字节变化为 −2594/+6127/−3657。ThinkThread 与 PR #1 冻结部分未修改。

原始 `completed child did not remain ephemeral` 偶发断言仍未确定根因。确定性屏障修复的是已独立复现的并行捕获提前清理缺陷，后续原生检查通过不能证明两者同源；历史时钟拒绝、夹具调试超时和不完整样本继续保留。

自整体代码净减少约束提出时的 `4c7dbb2` 至 `e76ecae`，`src/test/bench` 中代码合计减少 285 行、8,772 个 LF 字节；文档不计入。新增情况合并在现有测试内，保留正确回退、所有权和批次边界覆盖。ThinkThread/PR #1 冻结部分的限制只记录在现有问题说明中。

### 以 `caaf4ef` 为基线的持续审计

`b638103` 阶段修复了异步验证期间退休结果的迟到认领、持久历史失败丢失已封存证据、空闲目录退休未被关闭等待，以及 Pattern 前沿尚未准入就被跨轮跳过的问题。相同身份修订也确认准入；同会话动作回退只取权威样本，真实失配和匹配后拒绝更新原样本，淘汰、schema 替换、结束会话和复制令牌不污染现有反馈。原始 ephemeral 偶发断言仍无法归因：产物写入失败与事务时钟拒绝的独立注入均复现相同断言，诊断能区分其阶段、执行、作用域和可用证书，并在分支清理前保存验证及存储状态。它们不证明历史失败同源。

Drafter 在没有可用注册工具时跳过模型、配置和预热，同批候选共用完整上下文准入检查。4096 条消息、16 个候选的相邻模块对照中，本地准备至完成的中位数在 Windows 为 0.827 → 0.296 ms，WSL 为 0.852 → 0.266 ms；检查次数从 16 降至 1，过大上下文不再调用配置函数。完整请求、thinking、预算及 16 次 mock 请求不变，外部 API 为零；不把本地扫描收益算作 API 或费用节省。

Bash 动态依赖捕获改用本次 interposition 的可执行文件集合。插桩样本的重复成员扫描为 52/63 次 → 0，宿主路径捕获仍为 33/36 次；Bash/子进程的依赖捕获约 101/105 → 37/39 ms。每版各六次不插桩的 ready 对照中，完整 Bash 返回中位数约 16.33 → 16.35 ms，跨父子进程约 17.90 → 17.44 ms；只确认重复工作减少，没有稳定的完整任务提速证据。原生短 Bash 约 3 ms 的负结果保留。

两平台各用相反顺序运行两轮独立开关：Drafter、PatternAware、Actor 预览的八种组合，加上关闭投机的 Host 与完整 `caaf4ef` 编译基线，共 40 次长程严格回放。关闭投机仍经过 Host，不是完全不加载插件的纯 Actor 进程。Windows 保持 400/80 ms、25 次工具调用；WSL 保持 1500/80 ms、27 次工具调用及同一 C 子进程夹具。Actor 每次 20/22 个请求；开启 Drafter 时它也恰好请求 20/22 次。以下为两次完整耗时的范围，包含初始化、争用、执行、结算与回收，单位秒：

| 来源 | Windows | WSL | 文件采纳（Win/WSL） |
| --- | ---: | ---: | ---: |
| `caaf4ef` 全开 | 9.939–10.033 | 34.986–35.021 | 22/23 |
| Host 关闭投机 | 10.350–10.435 | 35.245–35.300 | 0/0 |
| 启用 Host，无来源 | 10.378–10.400 | 35.317–35.350 | 0/0 |
| 仅 Actor 预览 | 10.589–10.703 | 36.144–36.155 | 0/0 |
| 仅 PatternAware | 10.399–10.438 | 36.553–36.591 | 2/2 |
| 仅 Drafter | 9.940–10.009 | 34.701–34.748 | 22/23 |
| Drafter + PatternAware | 10.019–10.060 | 34.794–34.846 | 22/23 |
| Drafter + Actor 预览 | 9.910–10.002 | 34.810–34.976 | 22/23 |
| PatternAware + Actor 预览 | 10.640–10.765 | 36.413–36.504 | 2/2 |
| `b638103` 全开 | 9.929–10.040 | 34.862–34.873 | 22/23 |

各次完整工具输入/输出、后续请求 payload、阶段、thinking、tool choice、token 预算、usage、逐步文件效果和最终回复均一致，清理后的运行计数为零。预览与原生回退数量可以变化；逐调用核验唯一结算、提供结果的候选只启动和完成一次，以及回退恰好执行一次。所有含 Drafter 的 WSL 样本均保留一次跨父子进程复用，Actor 父命令仍执行；表中的文件采纳不包含这次子进程命中。Pattern 开启的所有样本具有相同 19/21 个实际批次成员与因果边界；成员比较排序副本，真实 Store 仍按各次原始到达顺序运行，仅固定 durationMs 为 1，全部后续前沿与学习状态一致。这项离线比较不注入执行反馈，不能用来抹掉运行时来源竞争带来的预测差异。

当前全开的预测条数为 76/87，基线为 120/155，全部文件采纳仍归于 Drafter。Pattern 独立开启时两端各两次采纳均为 `resource.inputs` 投影，精确预测命中为零。预测减少不等于实际执行减少：Windows 第一组中 Pattern 启动 9 → 11 次、失败 7 → 8 次、直接采纳均为零；当前失败执行的累计服务时长约 31.5 ms，区间可重叠且不包含执行前准备，不能作为任务额外耗时。当前全开两轮失败数为 Windows 8/8、WSL 13/10，WSL 基线为 18/22；仅 Pattern 的 WSL 第二轮仍有一次失败。全部负收益保留，不据两轮范围宣称稳定整体提速或自然预测改善。剩余工作是让失败的可恢复性、准备成本和真实采纳共同约束无效候选；不能把未观察的执行失败直接记作预测错误，也不能用关闭来源或针对这条 tape 调参来声称修复。

`adoption-latency.mjs` 在当前代码上另完成 340 个 Host 样本：每平台八类本地工具各 12 次 ready、8 次 running，WSL 的普通 Bash/跨父子进程另各 6 次 ready、4 次 running。每次新建 Host，使用固定 mock 提案和原生输出/文件效果对照，外部 API 为零。下表为不插桩 ready 的 p50，单位 ms；箭头表示同一绑定的原生执行 → 完整 `Host.execute` 返回。逐次生命周期另计准备、返回、结算和所属资源回收；每工具共享的搜索 profile 初始化/回收单独记录，oracle、夹具构建和正确性检查不计入。

| 工具 | Windows 原生 → Host | WSL 原生 → Host | Windows 逐次生命周期 | WSL 逐次生命周期 |
| --- | ---: | ---: | ---: | ---: |
| read | 0.429 → 1.031 | 0.383 → 1.237 | 8.712 | 5.584 |
| ls | 0.213 → 0.732 | 0.237 → 0.854 | 9.017 | 4.573 |
| write | 0.509 → 3.072 | 0.311 → 1.474 | 372.908 | 43.584 |
| edit | 0.737 → 3.271 | 0.663 → 1.698 | 373.216 | 43.808 |
| captured find | 4.048 → 1.086 | 3.689 → 1.020 | 11.446 | 7.336 |
| captured grep | 18.989 → 4.139 | 3.554 → 4.131 | 103.005 | 29.911 |
| native find（回退） | 14.889 → 15.015 | 7.803 → 8.182 | 15.318 | 8.462 |
| native grep（回退） | 11.075 → 11.651 | 6.489 → 6.742 | 11.955 | 7.032 |

六类合格文件路线每类 12/12 次 ready 采纳，原生搜索 0/12 次且原工具只执行一次。captured profile 的原生对照使用共同绑定，不能外推到默认 Pi 搜索。各阶段 p50 不能相加来重建生命周期 p50；原始报告逐次验证真实区间之和。小 read/ls/write/edit 及本次 WSL captured grep 的 ready 采纳仍比原生执行慢。

running 模式在 Actor 加入后释放受控生产者，并插桩区分剩余执行与完成后交付。Windows write/grep 的完整返回 p50 为 60.22/97.15 ms，剩余执行分别为 56.12/93.25 ms；WSL 为 11.98/32.22 ms，剩余执行为 10.28/28.18 ms。每类均 8/8 次采纳，全部原始样本和较慢尾部保留；这些等待控制样本不能视为自然模型领先时间或独立净提速。

WSL ready 普通 Bash 的原生/Host 返回/准备 p50 为 2.84/15.14/1625.07 ms，逐次生命周期 1648.92 ms；跨父子进程为 95.73/16.77/2240.09 ms，生命周期 2260.60 ms。running 的整个父 Bash 返回 p50 为 374.47 ms，原生为 90.37 ms，剩余生产者与完成后交付的各自 p50 为 352.33/11.74 ms，清理为 40.56 ms。每次子进程样本均保留原父命令执行；ready 六次命中、running 四次接续。C 计算、隔离准备与受控释放属于机制/成本测量，不能用约 17 ms 的 ready 返回掩盖完整准备或约 374 ms 的运行中等待。

该阶段 Windows 全套 629 通过、21 项平台跳过；WSL 全套 649 通过、1 项 Windows 路径规则跳过，共 650 项。代码与基准类型检查、两端构建通过。同版原生 exec 边界与 Runtime 运行中采纳检查通过，保留 completed/running 同轮一次消费、跨轮拒绝、描述符与元数据不匹配拒绝、改变输入回退及污染结果不持久化；原有 4 秒 sleep/3 秒领先夹具只验证机制。契约重构逐项核对公开类型、回调参数和编译结果；公共导出整理保留原符号及运行时值。相对 `caaf4ef`，`src/test/bench` 实际代码含 C、文档除外，净减 175 个物理行、236 个 LF 字节（分目录字节变化 −3248/+2098/+914）；没有删除有效回归或移动生产逻辑。ThinkThread 与 PR #1 冻结部分未修改。

### 准备所有权与后续资格（`68d5397`）

`3580f9b` 将权威观察后的可选准入准备交给会话任务持有。确定性屏障复现了慢指纹探测阻塞普通轮次关闭，以及尚未绑定的过期前沿令下一轮错误返回空提案；终止、禁用和关闭仍等待所属准备结束。插桩中一次轮次关闭从约 515 ms 降至 1.6 ms 内，后端准备仍执行，只是允许与 Actor 重叠。相邻版本每平台各 12 次完整回放中，仅 Pattern 的 WSL 两个配对减少 491–639 ms；全开、Windows 没有一致收益。完整请求与费用归属不变。

`09ce3c4` 将三个可执行文件摘要入口接到共用的稳定描述符读取，保留全部 SHA 字节、前后身份及释放检查。`/proc/PID/exe` 直接打开运行镜像，替换或删除路径不会改为读取另一个文件；普通路径捕获仍拒绝不合格链接。90 个独立摘要试验逐个核对完整哈希，126,458,664 字节 Node 镜像的中位数为整份读取 107.7 → 有界读取 73.3 ms，采样前后 ArrayBuffer 增量约 126.5 → 1.07 MB，这不是峰值内存测量。小 Bash/helper 镜像略慢约 0.1 ms。另 24 次完整回放中，WSL 全开两对减少 239–297 ms，Windows 无一致收益；失败候选仍保留，不作自然模型结论。

`2245fcf` 修正了“后端曾经准备过就可能有子进程结果”的过宽判断。真实生产者从打开前持有到执行、捕获、封存及排空完成；同轮临时结果在生产者关闭后仍由交接登记保留，存储查询中刚开始的生产者也不会漏掉。仅预热且空历史的后端直接运行 Actor。确定性用例覆盖重叠生产者、打开失败、取消、重复关闭及封存阶段的持有期；两端原生检查与 24 次完整回放通过。仅 Pattern 的 WSL Actor 拦截数为 7 → 1，两对完整任务减少 213–281 ms；全开不稳定，其中一个样本变慢约 1.26 秒且失去一次子进程命中。

这一子进程未命中样本保留为 `producer-readiness-after-all-wsl-2.json`。其原日志没有子进程发布时刻，不能归因于持有期修改。两次自然诊断都在查询前发布并命中；两个独立版本的发布屏障都证明：跨轮 Actor 查询时结果仍是 running，就拒绝接续且不等待，发布之后才成为合格的 completed 结果。六份报告的完整 Actor、请求、原始 Pattern 批次与消费所有权比较通过。该边界复现不能确定原样本的根因，也不能替代早先 ephemeral 断言的调查。

后续重构共用进程原型、作用域比较、错误与数值边界，配置和快照类型从原定义推导；逐项核对可选/只读字段、默认值属性、214 个 Pattern 配置输入、240 次门控状态转换和 144 对作用域。tape 的 UTF-8 修复以中文和 emoji 跨 1/2/3/5 字节块复现旧版错误，27 种角色/块宽组合逐项比较五种完整分析结果；宽度断言的原 54 个值保留。此处 tape 分析整理不属于 Actor 执行提速。

`68d5397` 与保存的 `2245fcf` 完整构建再做 24 次相反顺序回放。新增完整编译代码清单：基线 83、当前 85 个 JS/MJS 文件，包含所有新 helper；计时前和回收后确认哈希不变，不修改原计时或任务检查。每个 Actor 输入/输出、后续请求、thinking、tool choice、预算、usage、逐步文件状态、最终回复、原生次数、候选归属和零残留均通过；Pattern 仍比较真实批次成员及因果边界，再按各自原始顺序交给真实 Store，在 1 ms 受控成本下核对全部后续前沿和学习状态。两次完整时长范围如下，单位秒：

| 来源 | Windows `2245fcf` → `68d5397` | WSL `2245fcf` → `68d5397` |
| --- | ---: | ---: |
| 仅 Pattern | 10.369–10.832 → 10.447–10.570 | 35.572–35.790 → 35.488–35.509 |
| 全开 | 9.923–10.016 → 9.918–9.976 | 34.639–34.724 → 34.614–34.847 |
| Drafter + Actor 预览 | 9.899–9.998 → 9.865–10.012 | 34.623–34.643 → 34.526–34.653 |

全开的文件采纳仍全部来自 Drafter，为 22/23 次；含 Drafter 的全部 WSL 样本各保留一次跨父子进程命中，Actor 父命令仍执行。仅 Pattern 仍只有两次 `resource.inputs` 投影采纳、零精确预测命中。当前全开失败候选为 Windows 8/7、WSL 12/13，两次范围不足以证明重构带来稳定整体提速。原有负收益和慢尾部没有剔除；仍需减少文件被删除后同身份候选的重复准备，而不把未观察执行失败记作预测失配。

同版 20 轮原生 exec 边界与运行中采纳检查通过，保持原等待、领先、一次消费、跨轮、描述符、输入改变、作业控制与回收断言。当前全套共 659 项：Windows 637 通过、22 项平台跳过；WSL 658 通过、1 项 Windows 路径规则跳过。两端代码/基准类型检查和构建通过。相对 `caaf4ef`，实际代码净少 352 个物理行、48 个 LF 字节，分目录字节变化为 `src −7131 / test +6427 / bench +656`，含 C、文档除外；全部新增有效回归保留。资格报告为 `final-contract-qualification.json`，体积报告为 `code-size-68d5397.json`。ThinkThread 与 PR #1 冻结部分仍未修改。

### 重复失败退避试验（未进入生产）

在 `32eb5a0` 上试验了按任务、完整动作键、执行路线和活动父分支隔离的失败退避：连续两次准备失败后，每四次已获容量的生产机会探测一次。取消和容量拒绝不计失败，Actor 已认领的候选直接执行；权威写入切换历史代，迟到执行不能污染新代。17 个边界用例覆盖输出拒绝、恢复、身份隔离、取消、迟到失败、容量释放及父分支；试验全套共 676 项，Windows 654 通过/22 项平台跳过，WSL 675 通过/1 项路径规则跳过。

以 `68d5397` 完整构建为对照，两平台各用相反顺序运行两轮 Pattern/全开/无 Pattern，共 24 次长程回放。完整编译代码清单、Actor 输入输出、请求 payload/阶段/thinking/预算/usage、逐步文件效果、最终回复、原生次数和零残留均通过。原始 Pattern 批次按既有因果边界与真实到达顺序核对；原生 exec 边界和运行中采纳资格通过。以下为两轮完整耗时范围，单位秒：

| 来源 | Windows 基线 → 退避试验 | WSL 基线 → 退避试验 |
| --- | ---: | ---: |
| 仅 Pattern | 10.447–10.459 → 10.495–10.503 | 35.511–35.934 → 35.447–35.623 |
| 全开 | 9.962–9.972 → 10.010–10.013 | 34.642–34.771 → 34.716–34.856 |
| Drafter + Actor 预览 | 9.897–9.968 → 9.950–10.031 | 34.616–34.622 → 34.623–34.678 |

全开失败候选为 Windows `8/7 → 5/5`、WSL `11/11 → 9/7`，退避分别取消 `3/3`、`5/3` 次准备。文件采纳仍为 22/23 次，均来自 Drafter；含 Drafter 的 WSL 样本各保留一次子进程命中。仅 Pattern 仍是两次 `resource.inputs` 投影、零精确命中。Windows 全开两个配对均略慢，WSL 配对方向不一致，没有稳定整体提速证据。

独立即时恢复夹具还确认：同一输入失败两次后，在第三次准备前恢复可用，基线成功准备并采纳，退避试验取消准备并由 Actor 原生执行一次；完整输出和预测的真实匹配均保留。夹具由屏障控制，原生历史耗时显式设为估计值，只证明这一恢复取舍。首次夹具因未提供可解析的执行身份而被正确拒绝为 `action_not_keyable`，不完整日志保留；修正夹具身份后通过。

因此保留试验补丁与全部新增用例，生产代码恢复为原 `32eb5a0`。后续减少重复准备需要结合当前可验证的可用性或实际共享工作，并保留即时恢复反例；两次历史失败不足以证明下一次准备仍无用。本次阶段提交归类为 `docs`，不新增生产门控，也不宣称 API 请求、自然准确率或费用改善。全部原始失败、负收益与不完整试验保留。资格报告为 `preparation-backoff-qualification.json`，恢复报告为 `preparation-backoff-recovery.json`，归档为 `preparation-backoff-experiment/manifest.json`；完整补丁 SHA-256 为 `a2a3c48641782a6ec3f6806f03be127828087cb5cf94deb1534f5c57fb2be2a5`。生产代码相对 `caaf4ef` 仍净减 352 个物理行、48 个 LF 字节，试验副本没有进入生产构建。

### 原生搜索回退的输出对照

Windows 的一次旧版 `native-grep` 回退与另一个原生 oracle 进程以不同顺序返回相同两行，原基准因此失败。Pi 的原生搜索没有请求排序，两个独立进程的文本顺序不能充当转发正确性的证明。`adoption-latency.mjs` 现在逐项断言实际回退输入，在返回 Host 前保存完整原生输出，并核对 Host 原样交付；报告同时保留两份原始搜索输出，不排序、不改工具语义。捕获 profile 和其他工具继续使用独立原生 oracle，文件效果、生产者/原生次数、清理检查保持。回退输入断言与输出捕获计入 Host 时间。

修正后的 Windows/WSL 32 份双向版本与诊断开关报告通过，共 1728 次调用；包含 48 次 ready 子进程命中和 48 次 running 子进程接续。四次原生搜索顺序差异及最初失败报告均保留，详见 `optional-diagnostics-adoption-qualification.json`。该提交归类为 `fix`，修正基准对照，不宣称生产执行提速。

### 可选诊断的缓存开销

无 `onEvent` 订阅时，Runtime 现在跳过事件快照、缓存清单和诊断投影；策略反馈、来源观察/结算、任务时间线重置仍执行。需要策略候选描述时继续构造描述。订阅存在时在原状态转换处捕获事件，异步反馈只延迟交付，不延后快照。事件失败由原有有界队列隔离，移除外围重复的异步捕获。四种无订阅/正常/抛错/阻塞模式共同核对两次任务的采纳、反馈、学习、时间线和关闭；这是 `perf` 修改。

独立缓存夹具在每个版本中先完成显式 Actor 预览，再执行播种 Actor 调用，最后各采纳 read/ls 64 次，覆盖 2/64/512 个保留结果。Windows 的原生结果观察资格没有改变：首次夹具错误地假定其能保留原生结果，得到 0 个结果而失败；原始失败保留，修正后的两侧都使用封存输入的显式预览。最终实现的 16 份报告共 15,392 次 Actor 调用全部输出/文件状态一致、零模型请求和原生回退、最终零残留。512 项时两轮逐次配对的完整生命周期如下，单位 ms；计入初始化、全部预览/播种、128 次采纳、反馈等待、终止和回收，原生 oracle 与夹具构建在计时外。

| 订阅 | Windows 基线 → 当前（两轮） | WSL 基线 → 当前（两轮） |
| --- | ---: | ---: |
| 无 | 1674.54 → 1379.57；1458.03 → 1390.92 | 1396.84 → 1235.57；1444.77 → 1322.59 |
| 正常 | 1484.79 → 1457.27；1442.83 → 1480.21 | 1310.05 → 1279.82；1403.44 → 1291.11 |

无订阅时 Windows read/ls 的完整采纳 p50 为 `0.869/0.696 → 0.685/0.552` 和 `0.731/0.629 → 0.689/0.536` ms；WSL 为 `0.789/0.548 → 0.630/0.446` 和 `0.767/0.545 → 0.673/0.488` ms，仍高于相应约 0.2–0.37 ms 的原生工具。正常订阅的 Windows 生命周期与单工具结果仍有负收益。前一版使用事件构造/交付闭包的实现及其 16 份缓存报告一并保留，没有据局部配对声称默认 TUI 稳定加速。32 份两种实现的缓存证据共 30,784 次调用，资格为 `optional-diagnostics-cache-qualification.json`；该外部核对验证持有者、计数、完整 oracle、诊断交付和区间之和，各次 Host 输出与文件哈希/模式比较由哈希固定的原始驱动断言，瞬时 Host 输出和文件快照未另行序列化。

最终实现另有 32 份全部工具 Host 对照，共 1728 次调用，包含 48 次 ready 子进程命中及 48 次 running 接续；两版本使用相同生产者完成屏障，正常订阅不控制就绪时刻。原生搜索五次顺序差异完整保留。单工具并非全面改善：正常订阅的 Windows write 两轮 `2.91/2.97 → 3.11/3.14` ms，edit `3.32/3.29 → 3.42/3.46` ms；无订阅的 WSL read/ls 也未一致变快。WSL ready 子进程约 16.5–17.5 ms，而受控 running 父 Bash 返回约 371–394 ms，原生约 89–96 ms；初始化、生产准备、剩余执行均不能隐藏为采纳收益。running 使用分段插桩和受控释放，不能作为自然领先时间或应用加速。资格为 `optional-diagnostics-adoption-direct-qualification.json`。

与 `68d5397` 的 24 次严格长程回放逐项核对全部 85 个编译模块、Actor 输入输出、后续请求 payload/阶段/thinking/tool choice/预算/usage、文件效果、最终回复、原生次数、消费归属和清理；原始 Pattern 批次按实际成员、因果边界及各自原顺序的真实 Store 学习核对。两轮完整耗时范围如下，单位秒：

| 来源 | Windows 基线 → 当前 | WSL 基线 → 当前 |
| --- | ---: | ---: |
| 仅 Pattern | 10.454–10.503 → 10.448–10.489 | 35.504–35.790 → 35.534–35.589 |
| 全开 | 9.960–10.062 → 9.965–9.971 | 34.661–34.694 → 34.714–34.793 |
| Drafter + Actor 预览 | 9.875–9.946 → 9.951–9.976 | 34.588–34.619 → 34.568–34.623 |

全开两端文件采纳仍为 22/23 次，均来自 Drafter；含 Drafter 的 WSL 样本各有一次子进程命中。仅 Pattern 仍是两次 `resource.inputs` 投影、零精确预测命中。全开失败候选为 Windows `7/8 → 7/7`、WSL `13/8 → 12/10`。没有稳定整体提速、API 请求减少或自然预测准确率改善的证据；WSL 全开两对均略慢。保留前一实现的严格回放与全部不完整样本，不以改变等待或过滤负收益获得通过。

同版 20 轮原生 exec 边界和运行中采纳检查通过，原有一次消费、作用域、描述符、输入/元数据、污染与清理断言保持，3 秒领先仍仅是机制夹具。全套共 663 项：Windows 641 通过、22 项平台跳过；WSL 662 通过、1 项路径规则跳过。两端类型检查、基准检查、构建通过；后续私有视图类型清理的 85 个 JS/MJS 编译结果完全相同。资格为 `optional-diagnostics-direct-qualification.json` 和 `optional-diagnostics-direct-types-qualification.json`。此中间状态相对 `caaf4ef` 少 336 个物理行但多 2409 个 LF 字节，代码净减少要求尚未满足，仍需继续收敛。ThinkThread 与 PR #1 冻结部分未修改。

### 模型套件

模型套件选择 [Claw-SWE-Bench Lite](https://huggingface.co/datasets/TokenRhythm/Claw-SWE-Bench) 的真实问题，只取得选定 base commit；每次创建新的 detached 工作区，不把 gold patch 给 Agent。

先在环境中提供 `DEEPSEEK_API_KEY`，再显式运行需要模型/网络的阶段：

```sh
npm run bench:ablation -- --instance axios__axios-5316 --label baseline --speculation-disabled
npm run bench:suite -- --suite swe_diverse --repeats 3 --label speculative --candidate-limit 2
```

PowerShell 可以用隐藏输入设置密钥，完成后移除环境项：

```powershell
$env:DEEPSEEK_API_KEY = Read-Host -MaskInput "DeepSeek API 密钥"
npm run bench:ablation -- --instance axios__axios-5316 --label baseline --speculation-disabled
Remove-Item Env:DEEPSEEK_API_KEY
```

API key 只从环境读取，不写入产物或交给基准 shell 子进程。工作区与 JSON 默认位于系统临时目录，`--output-root` 可显式选定位置。`--prepare-only` 只检查数据集与新 checkout，不发模型请求。

默认预算为 128 轮；`turnLimitReached=true` 表示未完成。`--speculation-disabled` 关闭投机，`--drafter-disabled` 仅关闭 Drafter。`--drafter-max-depth N` 默认 1、0 为单步；后继沿用下一决策额度。`--drafter-disabled --pattern-aware --pattern-state <目录>` 隔离历史模式实验；显式状态目录共享逻辑仓库身份与学习结果，不共享工作区文件，省略时各次运行隔离。

模型 runner 使用生产 Host.execute、Git 事务和 TUI 同样绑定的 read/ls 封存输入路线。移除只延迟 Actor 回调的人工 `--latency` 档位；工具按实际耗时执行。原工具回调、Actor 预览和投机采纳分别计数。Native grep/find 和 Bash 保留 Actor，提前执行资格使用前述独立入口。

## 计时与验收规则

`actualEndToEndMs` 从工具/Host 初始化前计至终态结算、Host 与工作区回收完成；`setupMs`、`agentPromptMs`、`teardownMs` 构成这一总时长。数据集下载、checkout 和最终补丁检查在计时外。Drafter 根请求直接接收 Actor 即将提交的完整上下文，不自行重建首轮消息。

独立 Agent 对照使用纯 Actor+原版工具、关闭的当前 Host，以及开启的父版和当前 Host。提示词、schema、调用 ID、参数分片、完整输出与后续上下文须一致，仅归一化夹具路径与时间戳。父版提交、执行次数及逐轮结果随报告保存，[当前证据](../docs/bash-reuse-capability-lattice.md#当前证据与剩余工作)不沿用旧提交的样本量或收益。

独立进程从 `execFile` 计至真实退出，包含导入、首次 SDK 初始化、任务、回收与共同摘要；纯 Actor 不加载投机模块。夹具、oracle 和对称的 SDK 状态检查在父进程计时外执行。顺序提前平衡，保留全部负收益、原生回退与波动。构造流、API 为零，不等于自然模型、Pi TUI 或冷 OS 缓存性能。

采纳的主指标是完整 `Host.execute` 到结果返回的时长。候选已完成时直接测量；运行中候选分别报告接入时间、剩余执行时间和完成后的交付时间。准备能否隐藏取决于实际 Actor/Drafter 重叠，不能把启动和剩余执行混入替换开销，也不能把内部 `hitLatencyMs` 当成完整返回时间。

端到端净加速使用相同任务的独立关闭/开启对照：`disabledMs / enabledMs`，两侧都包含各自初始化和回收。`serializedCounterfactualMs / actualEndToEndMs` 只描述同次轨迹的重叠；它为 1×不表示低开销，大于 1×也不证明比关闭投机快。计算身份去重，未采用预测和旧任务缓存不计入，不能累加所有 `executionAheadMs` 代替重叠。

1. 固定任务、初态、模型、候选数和超时，独立测量开/关投机，计入失败、争用与清理。
2. 要求 `git diff --check` 通过并保留完成信息，比较完整时长、命中、重叠、工具工作量与模型成本。
3. 延迟比较要求 `patchCandidate=true`：低于轮次上限、无超时/Agent 错误、补丁非空且干净，并与 gold patch 文件有交集。
4. `patchCandidate` 只是筛选，不是正确性结论；仍须由数据集工具链/容器执行 `FAIL_TO_PASS`、`PASS_TO_PASS`。
5. 性能收益须重复验证，且结果、权限和回收不退化；安全修正单独说明必要性，负收益行仍保留。

套件串行运行，失败即停止，结果汇入 `suite-result.json`。分别汇总独立对照和同轨迹重叠，标明分子来源；95% bootstrap 按任务聚类，不拆散同任务重复，p95 使用最近秩。独立对照使用 `pairedLatencyStatistics`。

汇总命中率为总命中/总 Actor 动作。未通过 `patchCandidate` 的运行保留失败原因，但不纳入汇总延迟和命中率，仍不能据此代替官方正确性评分。

## 历史测量

重复阶段报告已移出当前目录；必要的边界和经验归入[能力说明](../docs/bash-reuse-capability-lattice.md)与[研究记录](../docs/bash-reuse-research.md)。保留 PR #1 的[发布说明](./results/release-qualification-2026-09-03.md)。其余原始历史报告仍可在[固定提交的 Git 历史](https://github.com/xchang1121/pi-speculative-action/tree/4c7dbb2/bench/results)查阅，不能当作当前源码的完整资格或性能保证。
