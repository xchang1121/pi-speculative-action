# 更新记录

## 当前版本

- Drafter、历史模式与 Actor probe 共用计划、调度和结算；完整工具批次保持请求归属、thinking、预算及取消所有权。
- 动作身份与执行证据分离。文件结果、封存输入查询及 Linux 子进程复用通过权限、作用域、新鲜度和一次消费检查。
- Bash 支持跨父命令的 completed/running 子进程接管；捕获、封存、提交和关闭共同管理资源生命周期。
- 文件工具共用受控输入与工作区事务；输入捕获、工作区预热和同次依赖验证减少重复工作。
- Native Pi 是默认搜索方式；Captured search 在 Actor 与投机两侧绑定同一执行器，保留明确的搜索语义和进程预算。
- TUI 分别显示预测、重放、观察和提前执行能力，配置在 Apply 后生效，缺失能力按层级降级。
- 任务加速比采用同次运行的反事实串行口径；组件成本、模型费用和自然任务效果分别验证。

使用方法与当前限制见 [README](./README.md)，维护中的验证入口见 [bench](./bench/README.md)。
