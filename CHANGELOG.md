# 更新记录

## 当前版本

- 工作区事务的前态同步与后态捕获共用文件状态更新，增量维护保留字节数，省去重复遍历。删除 `WorldReuseMetrics` 中六个 `actorTimed*` / `actorBaselineMs` 及对应 `wholeCommandActor*` 估算字段和状态栏推算；完整任务计时、进程命中记录与调度学习保留。
- Linux 进程资格并入现有测试入口，共用场景和资源回收；删除独立运行器、grep 成本采样及重复报告脚手架，保留搜索语义与进程接管证明。设置菜单共用选项和动作映射。
- 私有快照准备共用变化通知，省去整树捕获、复验及无用的原生窗口路径绑定收集；独立子进程复用同一工作区池的不可变准备结果，省去启动前重复整树验新。准备结果不授予采纳权，当前输入、路径、别名和效果仍通过原有完整证明；绑定文件操作通过提交事务验证实际输入与效果，可复用准备快照；未封闭输入的普通分叉继续整树验新，过期或外来准备身份回到原流程。
- Drafter、历史模式与 Actor probe 共用计划、调度和结算；PatternAware 可接续 Drafter 整批结果进行多步预测。真实结果触发的下一步准备纳入现有请求槽，跨轮保留并统一取消与回收。工具批次保持请求归属、thinking、预算及取消所有权。
- 动作身份与资源检索分离。整文件预测可通过注册的结果投影匹配局部读取；跨工具输入复用改用候选实际封存的资源索引，由当前绑定工具执行自己的语义，单列 `inputs` 采纳，不授予预测匹配或整项等价性。资源索引共用原缓存预算和生命周期，输入重算不复制未使用的源输出。私有目录准备只创建一次相同目录，已有原始配置不重复复制，筛选时改写的配置在执行前恢复。索引区分精确名称和可解析子路径的别名，普通目录元数据不触发无关子路径尝试；输入重算只检索当前绑定语义为观察的动作，整项匹配保持原契约。当前根解析、路径别名及缺失查询边界纳入原资源证明；新查询只验证实际使用且来源完整的输入，保留已有元数据证明，避免无关内容回读。封存不复读已拥有的受控输入，并发加入共用正在进行的验证；采纳独立验新，原生观察保留执行窗口证明，查询证明不授权原结果提交。
- Bash 支持跨父命令的 completed/running 子进程接管，现有 Plan 消费者可跨轮持有一次性计算，消费者取消或过期后跨轮资格失效；连续 exec 保留各次观察的完成归属，线程 exec 回收旧线程记录。原生边界与隔离执行共用上下文采集和输出路由；缺失标准流或未建模的继承 FD 在原生边界保留执行。捕获、封存、提交和关闭共同管理资源生命周期。运行中候选收到相关输入变化提示时，先比较已有事务前态与当前内容，只有字节不同才解码已有跟踪，确认子进程访问过该输入后提前放弃等待；无变化不扫描文件，仍以完整证据决定最终采纳。
- PatternAware 从 Linux 原生完成和真实采纳记录学习可撤销启动绑定，跨轮在新隔离工作区中预执行内部单元，省去无用的父命令 PATH 拦截准备；父工具执行时统一准备，取消和关闭等待准备资源回收。首次学习的映像校验与原生计算并行，未及时完成则取消并回收；绑定不授予结果采纳权。参数不从摘要推测，保留原始 `argv[0]`；一次性结果、启动绑定及整项工具分别管理。
- 文件工具共用受控输入与工作区事务；Git 快照隔离用户配置并保留原始字节，预热与回收由单一所有者管理。驱动选择复用准备快照，普通分配仍验证精确内容与暂存一致性；独立子进程共用内部事务前后态。OverlayFS 结构记录保留只读底层的实际字节来源，修复跨层身份混用导致的错误拒绝。
- Linux 隔离子进程由已有原生 helper 直接启动，移除中间 Node 启动层；输出路由与信号语义保持一致，使用现有上下文探测。隔离后端保留 `readlink` 的原始错误码，使 Node 原生 `realpath` 可正确解析普通文件和目录；回归检查复用安装时的现有 Node 进程，不增加工具执行时的验证。
- Linux 原生观察器覆盖 FD 控制调用，拒绝把隔离副本的锁、租约和共享标志状态当作真实资源证明。CPU 时钟和 ELF 启动随机输入沿现有一次性转交链路管理，不再从缺少对应 syscall 推断结果可重复；旧观察契约的进程证书自动失效。
- Native Pi 是默认搜索方式；Captured search 在 Actor 与投机两侧绑定同一执行器，保留明确的搜索语义和进程预算。Actor 可借用预测端的空闲工作进程，减少原生回退的重复启动；预测保留 Actor 的空闲容量，取消和关闭按当前调用归属处理。首次捕获的私有目录可随原资源版本保留，同一路径与 glob 的新查询复用目录准备和筛选；原始依赖证明、字节预算与回收由原资源所有者统一管理。
- TUI 分别显示预测、重放、观察和提前执行能力，配置在 Apply 后生效，缺失能力按层级降级。
- 任务加速比采用同次运行的反事实串行口径；输入重算只登记实际采纳的查询区间，不把来源工具未使用的结果计算重复计入，缓存保留同一计算身份。进程成本记录区分实际参数，避免混合廉价查询与昂贵计算。取消前的执行耗时进入原调度器的同动作成本下限；仅有取消记录的候选超出下限后按既有频率试探等待，避免把它当作即将完成。预测提前量与实际已执行时间分别计算，成功执行后更新估计；原生学习后的首次子进程等待保留未知成本，以 Actor 成本限定试探预算。组件成本、模型费用和自然任务效果分别验证。

使用方法与当前限制见 [README](./README.md)，维护中的验证入口见 [bench](./bench/README.md)。
