# 更新记录

## 当前版本

- Drafter、历史模式与 Actor probe 共用计划、调度和结算；PatternAware 可接续 Drafter 整批结果进行多步预测。真实结果触发的下一步准备纳入现有请求槽，跨轮保留并统一取消与回收。工具批次保持请求归属、thinking、预算及取消所有权。
- 动作身份与执行证据分离。学习、查找和反馈统一使用注册的投影规则，整文件预测可匹配局部读取；采纳仍检查内容覆盖。文件结果、封存输入查询及 Linux 子进程复用通过权限、作用域、新鲜度和一次消费检查。
- Bash 支持跨父命令的 completed/running 子进程接管，现有 Plan 消费者可跨轮持有一次性计算，消费者取消或过期后跨轮资格失效；连续 exec 保留各次观察的完成归属，线程 exec 回收旧线程记录。原生边界与隔离执行共用上下文采集和输出路由；缺失标准流或未建模的继承 FD 在原生边界保留执行。捕获、封存、提交和关闭共同管理资源生命周期。
- PatternAware 从 Linux 原生完成和真实采纳记录学习可撤销启动绑定，跨轮在新隔离工作区中预执行内部单元。首次学习的映像校验与原生计算并行，未及时完成则取消并回收；绑定不授予结果采纳权。参数不从摘要推测，保留原始 `argv[0]`；一次性结果、启动绑定及整项工具分别管理。
- 文件工具共用受控输入与工作区事务；Git 快照隔离用户配置并保留原始字节，预热与回收由单一所有者管理。
- Linux 原生观察器覆盖 FD 控制调用，拒绝把隔离副本的锁、租约和共享标志状态当作真实资源证明。CPU 时钟和 ELF 启动随机输入沿现有一次性转交链路管理，不再从缺少对应 syscall 推断结果可重复；旧观察契约的进程证书自动失效。
- Native Pi 是默认搜索方式；Captured search 在 Actor 与投机两侧绑定同一执行器，保留明确的搜索语义和进程预算。
- TUI 分别显示预测、重放、观察和提前执行能力，配置在 Apply 后生效，缺失能力按层级降级。
- 任务加速比采用同次运行的反事实串行口径；进程成本记录区分实际参数，避免混合廉价查询与昂贵计算。取消前的执行耗时进入原调度器的同动作成本下限，减少提前量不足时的重复启动，成功执行后更新估计。组件成本、模型费用和自然任务效果分别验证。

使用方法与当前限制见 [README](./README.md)，维护中的验证入口见 [bench](./bench/README.md)。
