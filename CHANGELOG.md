# 更新记录

## 当前实现

- refactor: 公共入口对已完整暴露的模块共用导出，逐项验证符号、类型与运行时值不变；请求上下文、绑定证据和 Host 配置从现有契约派生。Pattern、Runtime、进程证书夹具及基准报告共用默认值和实际统计结果，保留原断言与只读约束。
- chore: Host 采纳基准分别记录准备、完整返回、结算和所属资源回收，完整生命周期计时等待这些操作完成。
- fix: Linux 基准夹具在返回边界保留公开 AgentTool 类型，避免类型声明引用依赖内部的 schema 包路径；嵌套安装布局下的 WSL 基准检查通过，运行代码不变。
- refactor: 测试共用原生命令、路径投影与文本结果夹具，单调用 SSE 复用完整批次生成；物化计划与配对统计复用提案契约和实际统计结果，保留只读类型及全部断言。
- refactor: tape 的五种报告类型从实际计算结果派生，保留原只读形状；PatternAware 与 trace 的预测结算共用夹具，保留未观察、未匹配、拒绝和采纳断言。
- refactor: Runtime、Host、事务与 gateway 的测试共用分支夹具；执行身份、可选验新、提交/回收回调和显式覆写保持各案例原语义。
- chore: 清理旧报告驱动的准入 CLI 和 PR #1 之后添加的旧 ThinkThread 基准入口；完整采纳共用现有 Host 基准，本地六工具语义夹具移除无消费者的计时。重复阶段报告从当前树删除，保留经验、固定 Git 历史链接和 PR #1 的发布说明。
- refactor: 合并路径集与大产物的成本基准，共用原生夹具、计时和指标差值；计时等待分支回收完成，保留完整输出、产物、新鲜度和消费检查。生成的四份 C 程序与整理前逐字节一致。
- refactor: exec 边界基准共用生产分支、Actor 计时与等待助手，保留原等待预算、轮次作用域和异步回收；测试共用自有临时目录、进程原型与模型夹具，目录位置、模型参数和各套件的清理顺序保持一致。

### 动作身份与预测

- fix: PatternAware 仅携带 Runtime 已接受的预测前沿；同身份修订也确认准入，关闭期间或绑定失败的机会可在下一轮重新提出，迟到回调不修改新批次。
- fix: 同会话动作回退的真实失配与匹配后拒绝进入原样本的衰减反馈和排序；会话结束、淘汰、schema 替换和复制令牌不污染现有样本，不将未观察失败记作预测错误。
- perf: 动作回退以权威 Actor 样本为依据，停止从假设历史递归重复生成回退；绑定求值与去重共用实现。
- perf: Drafter 没有已注册的可用工具时跳过模型、配置和预热；同批候选共用完整上下文准入检查，请求温度、thinking、工具批次及费用归属保持独立。
- perf: 提案和物化通知共用可选元数据复制，减少逐字段临时对象；只省略 undefined，保留两种通知各自的字段顺序、零值和输入引用。
- fix: Runtime 的请求与其准入候选共同持有一个取消令牌，空响应、参数无效或准入拒绝后及时退休；有效候选、同伴请求及多步续推沿用各自期限，后台准备不再脱离所有者。
- fix: Drafter 的准备持有期覆盖提案结果；空批、无效批与失败响应及时退出共享预热，有效提案和存活同伴继续持有。共享取消信号的请求分别计数，提前退休仍排空已经开始的准备操作。
- perf: PatternAware 先对绑定组合保留原概率与稳定顺序下的有限前沿，再构造选中的嵌套输入，减少最终丢弃的对象复制；全部绑定结果、预测和学习状态保持一致。
- fix: Drafter 模型与配置准备由整批仍存活的候选共同持有；单个候选取消不误杀同伴，全部取消或批次退休后不再进入下一准备阶段。预热沿用批次生命周期，仍等待已启动的模型/配置操作排空；实际请求和收益结算保持各自归属。
- fix: tape 的 Drafter 竞速统计保留获胜响应的完整工具批次，覆盖按动作去重、费用按请求计量；宽度与竞速共用覆盖计算，上下文配对共用实现，工具与流形状共用一次 SSE 解码。
- refactor: PatternAware 的观察与假设批次共用自有输入、批次校验和稳定排序，同批学习只生成一次历史 token；历史基准支持显式批次，并保持扁平事件的原有顺序语义。
- fix: Drafter 保留同一响应的完整工具批次；结果在占用 continuation 额度前收集，全部成功执行后按原顺序续推一次，后继依赖整批父动作。重复回调不重复请求，混入未启用工具或重复调用 ID 的批次不发布；模型基准按整批计调用、按请求计 usage。
- perf: PatternAware 在同一后缀节点共用历史和输出形状，匹配直接返回所属模式；学习和裁剪使用连续批次起点，省去逐事件索引、重复展开和过滤。PPM 上下文仅在需要估计时构造，模式顺序、输出形状通配和批次因果边界保持一致。
- fix: Drafter 在批次边界独立确定 thinking effort 和输出预算；尊重 `getDraftOptions` 的显式 effort，续轮沿用。thinking 请求使用 `tool_choice: auto`，避免 DeepSeek 拒绝强制工具调用；默认仍不继承 Actor 的思考强度或 token 上限。
- Drafter、Actor probe 和 PatternAware 共用计划、调度与结算。参数准备一次，仅自有无损纯数据树可生成 K(a)；不支持的值保留 Actor 原调用及错误语义。新鲜度另行证明，预测命中仍要求请求覆盖。
- 计划先捕获不可变输入、再按修订准入；父语义变化重绑后代，Actor 借用保留结算权。查询先筛节点，再读取当前依赖状态。
- 候选 ID 单一登记，预测与预览同步认领，验新后核对当前代。仅有效需求参与调度，合并生产者启动批次，无主的未启动工作退役；共享结果可保留。不同输入版本与独立副作用各自持有所有权。
- Drafter 首个完成校验和执行绑定的有效动作可取消同伴；迟到绑定不再发布，后继沿用下一决策额度。请求与跨轮收益更新所属滚动样本，未发请求不计数，退休样本不复活。
- Actor probe 只派生自权威流，与 Drafter 共用预算。未知收益不记零、不强制 Actor 重跑；历史回退耗时只作参考，执行领先与目标 token 验收分别计量。
- PatternAware 共用后缀、间距与绑定分析。入库事件和公开快照独立持有；库内派生缓存按对象有界，公开推导不复用可变参数的旧缓存，续推历史不随后续观察漂移。现有测试覆盖这些所有权边界及含 LLM 间隔的多步续推。
- 仅已准入的 Drafter/sidecar 请求预热已注册工具；取消贯穿模型返回与准备，共享操作按各自所有权回收。

### 执行、证据与事务

- fix: 进程交接在异步验证后重新确认登记与存活状态，退休、淘汰或关闭的结果不能被迟到认领。持久历史写入失败保留已经封存的同轮结果和父分支证据，已执行进程不因录制失败而重跑。
- fix: 工作区关闭等待已经进入空闲退休的目录清理；相同根目录的新池和其他所有者保持独立，清理错误沿原生命周期报告。
- perf: Bash 动态依赖捕获使用本次 interposition 的可执行文件集合查询成员，省去逐依赖扫描 PATH 条目；不缓存可变身份或省略依赖证明。
- chore: 封存失败记录具体阶段、请求、作用域、工作区及可用证书信息；exec 基准在清理前保存失败旁证。原始 ephemeral 偶发断言仍未确定根因。
- fix: Bash 的 PATH 准备在取消后停止接纳新目录和可执行文件探测，排空已开始的一批操作后回收工作区，不再启动 broker。
- fix: 子进程的 trace 读取与工作区事务封存并行进行；任一捕获失败后先排空另一侧，再清理目录与返回已执行输出，禁止捕获失败触发重复执行。
- perf: 同一次子进程 handoff 查找在持久层跳过已尝试证书的磁盘副本，省去重复读取和验证；其余历史仍独立验新，排除身份不跨查找保留，也不授予复用权限。
- perf: 同一准备信号内重叠的工作区预热共用基线验证，完成或失败即退役；不同所有者、后续预热与实际 fork 仍独立验新，取消保留各自资源所有权。
- perf: 私有 Git 基线只验证实际快照覆盖的树，排除根目录及嵌套 `.git` 元数据，避免无关对象扫描和基线重捕获。排除规则归管理器所有；普通工具证据与可见链接目标仍完整验证，局部快照不能授权读取或宿主执行窗口。
- refactor: 私有工作区的 commit、tree 与验证凭证共同持有和替换，统一基线发布与回收，省去重复查询已知 tree 的 Git 进程。
- fix: 增量刷新私有 Git 基线时保留未变条目的 stat 数据，消除修改一个文件后整树反复误报变更的问题；沿用完整内容验新，并保留源工作区和用户索引。
- fix: 调度器保留未知服务耗时和未知 Actor 时序，只有实测时序与已知服务成本才推迟未来动作；移除由排序默认值生成的虚构周期，避免冷启动工作失去执行领先。
- fix: 同轮已完成子进程结果按批查询，共用路径集的历史状态只捕获一次当前依赖；仍先查内存，失败再查持久层。按规划器返回的证书认领，竞争失效后重新验证剩余候选，保留作用域、生产权限和一次性消费约束。
- perf: 缺失资源的祖先查询在单次证明中合并仍在进行的 I/O，结算后立即丢弃；精确采纳验证不等待只用于宿主观察窗口的 watcher 事件。工作区独占创建的新文件直接写入，省去重复 truncate，现有文件仍先验身份再清空。
- Router 统一选择执行环境，K(a) 不随隔离路线改变；ActorAction 统一候选选择、准入释放与权威结算，选中后禁止回退或迟到拒绝。
- ThinkThread 按需加载 SDK；根普通文件以封存输入驱动原版 Pi 操作，保留当前权限、祖先绑定、图像设置及协议预算。其余输入、父检查点与自定义执行器仍用 `fs.run`。
- 流式参数完整后严格解析，提示发出后停止累积；Actor 已到达则跳过新准备。预览各自持有兴趣，单方取消不撤销其他消费者的工作；采纳仍按完整 K(a) 授权、验新与提交。
- 文件绑定按需构造能力，token 的字节、目录名与派生元数据共用证据；Linux 以 O_PATH 验明文件类型后读取。关闭拒绝新读取，排空执行、句柄、所属 worker 与事务；错误、取消或未排空不发布。
- 工作区在异步边界前持有输入与效果副本，同锁验新后提交；检查点仅公开身份。新目录保留 Actor 继承策略，缺少观察权限证明则回退。
- 原生 helper 保留父退出状态并等待全部后代；helper/strace 被杀时终止所有 tracee，包含独立进程组。strace 缺失或截断的退出记录禁止封存；旧 helper 与旧证书不满足当前资格。
- 可执行路径有界准备，不缓存可变身份；Sandlock 优先使用 O_PATH 解析，ELF 分类不消耗脚本输入。Runtime 与安装器检查真实脚本资格。
- `createExecPrototype` 返回已校验且自有的规范原型。进程消费者接收在调用边界生成一次的 `weakKey`，不重复规范化，也不把键当作执行许可。
- 父分支与子进程共享一次性消费权，防止同一随机结果被整体和子进程重复采纳；仅明确回滚成功才恢复资格。可重复证书仍可复用，重放前持有完整输出/效果闭包。
- 原子发布只回收成功独占创建的临时文件；GC 与失败的并行维护必须由原有执行生命周期等待。
- 共享事务与投影各自持有副本，保留可枚举 Symbol；不可共享值不复用。事务与 ThinkThread 排队逐请求取新证明，提交前过期可干净回退；关闭排空队列，独占写入不提前提交。
- 启动沿用采纳收益判定：具备精确 Actor 样本、明确时延与调用时序时，预计无益则不执行、不抢占；信息不足仍试探。采纳验证等价性、权限、新鲜度与生产者保证，转换与缓存查询分别采样。未知 Actor 耗时不设零等待预算，稀疏或宽泛样本不否决已完成候选。干净拒绝允许一次 fallback；不确定或部分提交失败禁止重跑。

### 搜索与界面

- Native Pi 是默认搜索方式；TUI 的 Captured search 显式共同绑定 Actor 与投机执行器。
- 受控 find 复用 Pi 已有匹配/忽略组件；grep 只使用已验收的现有 rg，不新增安装或下载。缺少 rg 不影响受控 find。
- 搜索输入包含被消费的原始字节、具名祖先/Git 配置、正负元数据和 HOME 身份；原版 Pi 负责完整输出、上下文和截断。
- 该 profile 明确排序并禁用环境 rg/全局 ignore 配置，不宣称等价于原生默认。绑定失败不暗中换回另一执行器。
- TUI 区分预测、重放、观察与提前执行能力；设置在 Apply 后生效，刷新与关闭等待真实操作完成。

### 验证边界

- docs: 补齐 Windows/WSL 含写入长任务的三轮完整 mock replay：净时延分别减少约 0.81%/0.90%，WSL 跨父命令 Bash 为 852 → 32 ms；保留领先不足、首次 edit 等待和构造时序的限制，不声称自然模型普遍提速。
- docs: 合并历史审计经验，清除过期阶段报告；采纳入口延迟、独立开关净加速和同轨迹重叠分别计量，当前能力说明保留尚未解决的 ThinkThread 写入边界。
- 当前基线、完整测试、性能与体积见[验收说明](docs/bash-reuse-capability-lattice.md#当前证据与剩余工作)，命令见[基准说明](bench/README.md)。
- 对照纯 Agent+原版工具、关闭投机的 Host 与启用投机，校验上下文和流事件，计时包含初始化、拒绝与回收。构造流留仓库外，API 为零，不作自然模型性能证据。
- Windows x64 与 WSL Linux x64 的受控搜索及脚本驱动 TUI 已验收；性能测量与普通回归分开，不把候选就绪等同于有收益。
- Linux Bash 保留整体、子进程和运行中复用。空历史且后端未启动时直接运行 Actor；迟到证书、准备中的后端和不确定目录仍走完整检查，显式刷新执行功能自检。缺少 Landlock 时仍可消费合格证书；独立 Actor 未命中证书生产未完成。
- ThinkThread alpha4 x86_64 有部分读取采纳、冲突、取消、回退和 TUI 证据；写入的权限/文件身份偏差未修复。其余平台与完整资格仍见验收说明。
